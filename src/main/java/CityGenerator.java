
public class CityGenerator {

    // TODO don't know if we actually need this this class
    public static Tile[][] create(CityType cityType, int cityWidth, int cityHeight) {
        return null;
    }
}
